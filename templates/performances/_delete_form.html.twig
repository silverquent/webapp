<form method="post" action="{{ path('app_performances_delete', {'id': performance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ performance.id) }}">
    <button class="btn">Delete</button>
</form>

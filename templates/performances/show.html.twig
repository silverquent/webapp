{% extends 'adminBase.html.twig' %}

{% block title %}Performances
{% endblock %}

{% block body %}
	<!-- Content Wrapper. Contains page content -->
	<div
		class="content-wrapper">
		<!-- Content Header (Page header) -->
		{% include "breadcrumb.html.twig" %}
		<!-- Main content -->
		<section class="content">
			<div class="card">
				<div class="card-header">
					<h3 class="card-title">Listes exercises</h3>
				</div>
				<!-- /.card-header -->
				<div
					class="card-body">
					<!-- / Met le code ici du tableau  -->


					<h1>Performances</h1>

					<table class="table">
						<tbody>
							<tr>
								<th>Id</th>
								<td>{{ performance.id }}</td>
							</tr>
							<tr>
								<th>Resultat</th>
								<td>{{ performance.resultat }}</td>
							</tr>
						</tbody>
					</table>

					<a href="{{ path('app_performances_index') }}">back to list</a>

					<a href="{{ path('app_performances_edit', {'id': performance.id}) }}">edit</a>

					{{ include('performances/_delete_form.html.twig') }}

				</div>
				<!-- /.card-body -->


			</div>
			<!-- /.card -->
		</section>
		<!-- /.content -->
	</div>
	<!-- /.content-wrapper -->
{% endblock %}
